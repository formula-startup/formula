- content_for(:html_title) {'電話番号の確認'}
= render partial: 'signup-header2'
.signup-container
  %h2.signup-header 電話番号認証
  .registration-form
    = form_for @profile, url: sms_confirmation_signup_index_path,method: :post do |f|
      .signup-form-container.registration-form__first-container
        .signup-form-container__title
          = label :sms_number, '認証番号', class: 'signup-label'
        = f.text_field :tel, placeholder: '認証番号を入力', class: 'default-input'
      %p.registration-form--text SMSで届いた認証番号を入力してください
      = f.submit '認証して終了',{class: 'submit-btn submit-btn-red'}
= render partial: 'simple-footer'